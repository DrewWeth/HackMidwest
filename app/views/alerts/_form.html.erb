<%= form_for(@alert) do |f| %>
  <% if @alert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alert.errors.count, "error") %> prohibited this alert from being saved:</h2>

      <ul>
      <% @alert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.text_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :send_datetime %><br>
    <%= f.datetime_select :send_datetime %>
  </div>
  <div class="field">
    <%= f.label :is_sent %><br>
    <%= f.check_box :is_sent %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.check_box :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
